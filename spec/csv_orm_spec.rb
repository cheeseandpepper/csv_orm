require "spec_helper"

RSpec.describe CsvOrm do
  it "has a version number" do
    expect(CsvOrm::VERSION).not_to be nil
  end

  xit "does something useful" do
    expect(false).to eq(true)
  end
end
